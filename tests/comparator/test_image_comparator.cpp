#include "gtest/gtest.h"
#include "Comparator/ImageComparator.h"

TEST(ImageComparatorTest, CompareImages) {
    // ImageComparator comparator;
    // std::string image1 = "path/to/image1";
    // std::string image2 = "path/to/image2";

    // double result = comparator.compare(image1, image2);

    // ASSERT_NEAR(result, 0.0, 0.1);  // Example assertion
}